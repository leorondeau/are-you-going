import React from 'react'


export const User = ({user}) => (
        // {console.log("USER" , user.name)}
    <section className="user">
        <h3 className="user__name">User: {user.name}</h3>
    </section>

)
